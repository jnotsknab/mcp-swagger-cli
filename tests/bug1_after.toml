# AFTER (FIXED): Description properly sanitized
# This parses correctly as valid TOML

[tool.poetry]
name = "test_special"
version = "1.0.0"
description = "Description with newlines and \"double quotes\", \\backslashes\\, and tabs."
readme = "README.md"
authors = ["Generated by mcp-swagger-cli"]

[[tool.poetry.packages]]
include = "test_special"

[tool.poetry.scripts]
test_special = "test_special.main:main"

[tool.poetry.dependencies]
python = "^3.10"
mcp = ">=1.0.0"
httpx = ">=0.27.0"

[tool.poetry.group.dev.dependencies]
pytest = ">=8.0.0"
pytest-asyncio = ">=0.23.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"